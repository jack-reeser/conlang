package morph

// InflectionRule represents a rule that embeds inflectional information.
type InflectionRule func()

// FormationRule represents a rule that governs lexeme combinations.
type FormationRule func()
